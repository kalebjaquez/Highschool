#include <iostream>
using namespace std;

int main() {
const int size = 5;
int number [size];
int i; 

for(i = 0; i < size; i++)
{
    cin >> number [i];
}
for(i = size - 1; i >= 0; i--)
{
    cout << number [i] << endl;
} 
    return 0;
}
